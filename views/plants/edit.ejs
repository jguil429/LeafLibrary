<% layout('layouts/boilerplate')%>
    <div class="row">
        <h1 class="text-center mb-3">Edit Plant</h1>
        <div class="col-6 offset-3">
            <form action="/plants/<%= plant._id %>?_method=PUT" method="POST" novalidate class="validated-form">
                <div class="mb-3" >
                    <label class="form-label" for="common">Common Name</label>
                    <input class="form-control" type="text" id="common" name="plant[common_name]" value="<%= plant.common_name %>" required>
                </div>
                <div class="mb-3">
                    <label class="form-label" for="scientific">Scientific Name</label>
                    <input class="form-control" type="text" id="scientific" name="plant[scientific_name]" value="<%= plant.scientific_name %>" required>
                </div>
                <div class="mb-3">
                    <label class="form-label" for="duration">Duration</label>
                    <input class="form-control" type="text" id="duration" name="plant[duration]" value="<%= plant.duration %>" required>
                </div>
                <div class="mb-3">
                    <label class="form-label" for="date">Date Repotted</label>
                    <input class="form-control" type="date" id="date" name="plant[date_planted]" value="<%= plant.date_planted %>" min="1900-01-01" max="<%= new Date().toISOString().split('T')[0] %>" required>
                </div>
                <div class="mb-3">
                    <label class="form-label" for="image">Add Image(URL)</label>
                    <input class="form-control" type="text" id="image" name="plant[image]" value="<%= plant.image %>" required>
                </div>
                <div class="mb-3 text-center">
                    <button class="btn btn-success" >Update Plant</button>
                </div>
                <div class="mb-3 text-center">
                    <a href="/plants">All Plants</a>
                    <a href="/plants/<%= plant._id%>">Back to Plant Details</a>
                </div>
            </form>
        </div>
    </div>
    
